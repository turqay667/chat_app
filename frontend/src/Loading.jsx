const Loading=()=>{
    return (
        <div className="spinner-border text-info load" style={{width:50, height:50}}>
<span className="sr-only">Loading ...</span>
        </div>

    )
}
export default Loading;